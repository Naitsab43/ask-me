import styles from '../styles/profile.module.css'

export const NotQuestions = () => {
  return (

    <div className={styles["not-questions"]}>

      <p className={styles["not-question__text"]}>No hay preguntas para mostrar :(</p>

    </div>

  )
}
